{
  "name": "@omadia/channel-discord",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "main": "dist/plugin.js",
  "description": "Discord channel for the Omadia harness — discord.js Gateway bot with bot-token auth. Routes Discord DMs, @mentions and the /ask slash command into the Omadia orchestrator.",
  "license": "MIT",
  "homepage": "https://github.com/byte5ai/omadia-channel-discord",
  "repository": {
    "type": "git",
    "url": "https://github.com/byte5ai/omadia-channel-discord.git"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build-zip.mjs",
    "clean": "rm -rf dist out"
  },
  "dependencies": {
    "discord.js": "^14.16.0"
  },
  "peerDependencies": {
    "@omadia/channel-sdk": "*",
    "@omadia/plugin-api": "*",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "express": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
